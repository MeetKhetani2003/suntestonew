import { CheckoutStorefront } from "@/components/Storefront";

export const metadata = {
  title: "Secure checkout | Sustento",
  description: "Complete your Sustento order securely.",
};

export default function CheckoutPage() {
  return <CheckoutStorefront />;
}
